&#x20;  1. Понять сколько токенов тратит целая программа за один сеанс записи совещания до получения полноценного отчета. Выбрать модель LLM для данной задачи (по бюджету и эффективности).



&#x20;  2. Рассмотреть вариант решения вопроса: Как исправить «нестыковки» спикеров перед созданием отчета? Проинструктируйте Cursor добавить в промпт для LLM (GPT-4o) этап «очистки ролей».

&#x20;  Примеры дополнительного PROMT.

«Внимание: в транскрипте могут быть ошибки разделения спикеров (иногда фразы Speaker 0 на самом деле принадлежат Speaker 4 и наоборот).

Твоя дополнительная задача:

Используй логику диалога, чтобы понять, кто на самом деле говорит фразу. Например, если Speaker 0 задает вопрос, а следующая фраза Speaker 0 выглядит как ответ — скорее всего, это ответ Speaker 1.

Если в тексте звучат имена (например, "Алексей, как дела?"), используй их для точной идентификации, кто есть кто.

При заполнении JSON-отчета опирайся на логический смысл беседы, а не только на метки "Speaker".»

&#x20;  «Перепиши функцию диаризации. Сделай её динамической: программа не должна заранее знать количество участников. Используй автоматическую кластеризацию голосов. После получения транскрипта, добавь шаг идентификации спикеров через GPT-4o (пусть модель попробует определить имена участников по контексту их приветствий или обращений друг к другу) и только после этого заполняй финальный JSON-шаблон.»

&#x20;

!!! Важный момент: Чем больше спикеров, тем больше «галлюцинаций» у дешевых моделей. Поэтому для встреч, где больше 3-4 человек, я крайне рекомендую использовать только модель large-v3, так как она лучше всех различает наложения голосов (когда люди говорят одновременно).



&#x20;  3. Нужно исправить логику программы: OpenAI Whisper API -режет файлы по 20 МБ. Что создает кучу файлов (если овещание шло 2 часа). Раздувает вес на диске. Может потеряться логика обсуждения (т.к. на совещании может быть несколько человек).



&#x20;  Решение вопроса (для Cursor): «Добавь в мою программу функцию автоматической постобработки записанного WAV файла.

* Используй библиотеку faster-whisper с моделью large-v3 для транскрибации.
* Реализуй разделение на спикеров (их число в каждом совещании может варьироваться).
* После получения текста, программа должна автоматически отправить этот текст и загруженный ранее JSON-шаблон в OpenAI API (GPT-4o), чтобы сформировать финальный отчет.

Сохрани результат в файл 'final\_report.json'



&#x20; ВАЖНО: нужно решить задачу таким образом чтобы обработка звукового файла в текстовый не забирала много ресурса моего ультрабука. Например: Hugging Face Spaces - Вы можете бесплатно выложить свой код с моделью Whisper туда. Он даст вам постоянный API-адрес. Ваша программа в Cursor сможет отправлять туда звук и моментально получать текст. 



&#x20;  4. В программе нету функции, которая позволяла бы загрузить отдельно звуковой файл (например с другого/прошлого совещания), и в последующем произвести анализ и выполнить отчет совещания по загруженому раннее JSON шаблону. Т.е. сейчас я могу записать совезание и только после записи сделаю отчет. А если у меня уже есть запись совещания, то я не могу его загрузить, чтобы сделать отчет.

